<div class="card">
  <div class="card-header">
    <h1><%= t('expenses.edit')%></h1>
  </div>
  <div class="card-body">
    <%= render 'form', expense: @expense %>
  </div>
  <div class="card-footer">
    <%= link_to t('common.show'), @expense, class: "btn btn-info" %>
    <%= link_to t('common.back'), expenses_path, class: "btn btn-secondary" %>
  </div>
</div>