<div class="card">
  <div class="card-header">
    <h1><%= t('spenders.edit')%></h1>
  </div>
  <div class="card-body">
    <%= render 'form', spender: @spender %>
  </div>
  <div class="card-footer">
    <%= link_to t('common.show'), @spender, class: "btn btn-info" %>
    <%= link_to t('common.back'), spenders_path, class: "btn btn-secondary" %>
  </div>
</div>